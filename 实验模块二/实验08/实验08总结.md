# 实验08总结 - 模拟鼠标和键盘操作

## 实验概述

**实验目标：** 掌握ActionChains类模拟鼠标操作和焦点管理  
**实验时间：** 2026年5月12日  
**实验环境：** Chrome + Selenium + Python

## 实验内容

✅ 所有12个步骤执行成功

### 核心操作

1. **鼠标操作** - 使用ActionChains类
   - 移动到元素
   - 单击元素

2. **键盘操作** - Tab键切换焦点

3. **焦点管理** - 获取和操作当前焦点元素

## 关键技术点

### ActionChains类 - 鼠标操作

```python
from selenium.webdriver.common.action_chains import ActionChains

# 创建ActionChains对象
actions = ActionChains(driver)

# 单击元素
actions.click(element).perform()

# 移动到元素
actions.move_to_element(element).perform()

# 移动到元素并单击
actions.move_to_element(element).click().perform()

# 双击
actions.double_click(element).perform()

# 右键单击
actions.context_click(element).perform()

# 拖拽
actions.drag_and_drop(source, target).perform()

# 悬停
actions.move_to_element(element).perform()
```

### 焦点管理

```python
# 获取当前焦点元素
current_element = driver.switch_to.active_element

# 在焦点元素上输入
current_element.send_keys("text")

# 在焦点元素上按键
current_element.send_keys(Keys.ENTER)
```

### Tab键切换焦点

```python
# 按Tab键切换到下一个元素
element.send_keys(Keys.TAB)

# Shift+Tab切换到上一个元素
element.send_keys(Keys.SHIFT + Keys.TAB)
```

## 实验流程

```
1. 打开首页
2. 点击登录链接（ActionChains.click）
3. 移动到用户名框并单击（move_to_element + click）
4. 输入用户名"vip"
5. 按Tab键 → 焦点移到密码框
6. 等待2秒
7. 在焦点元素（密码框）输入"vip"
8. 按Tab键 → 焦点移到登录按钮
9. 等待2秒
10. 在焦点元素（登录按钮）按回车
11. 等待5秒
12. 关闭浏览器
```

## ActionChains vs 直接操作

### 直接操作

```python
# 直接点击
element.click()

# 直接输入
element.send_keys("text")
```

### ActionChains操作

```python
# 模拟鼠标点击
actions = ActionChains(driver)
actions.click(element).perform()

# 模拟鼠标移动
actions.move_to_element(element).perform()
```

### 何时使用ActionChains

✅ **推荐使用ActionChains：**
- 需要模拟真实鼠标移动
- 悬停操作（hover）
- 拖拽操作
- 复杂的鼠标操作序列
- 右键菜单操作

❌ **不需要ActionChains：**
- 简单的点击操作
- 简单的输入操作
- 性能要求高的场景

## 实验结果

```
✅ 成功点击登录链接
✅ 成功移动到用户名框并单击
✅ 成功输入用户名
✅ Tab键成功切换焦点到密码框
✅ 成功在焦点元素输入密码
✅ Tab键成功切换焦点到登录按钮
✅ 成功在焦点位置按回车登录
```

## 经验总结

- ActionChains需要调用perform()才会执行
- 焦点元素可以直接接收键盘输入
- Tab键是切换焦点的标准方式
- 获取焦点元素：`driver.switch_to.active_element`
- ActionChains适合模拟真实用户操作

## 运行方式

```bash
python 实验模块二\实验08\实验08_鼠标键盘操作.py
```

---

**实验状态：** ✅ 已完成
